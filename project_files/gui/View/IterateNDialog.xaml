<Window x:Class="gui.View.IterateNDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:helper="clr-namespace:gui.View.Helper"
        xmlns:local="clr-namespace:gui.View"
        mc:Ignorable="d"
        Title="Iteration count" Height="100" Width="180">
    <StackPanel HorizontalAlignment="Center"
                VerticalAlignment="Center">
        <TextBlock Text="Number of iterations:"
                   Margin="{StaticResource LeftPropertyMargin}"/>
        <TextBox Margin="{StaticResource RightPropertyMargin}"
                 x:Name="IterationTextBox"/>
        
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Name="Ok"
                    Width="45"
                    Click="OnOkClick">Ok</Button>
            <Button Name="Cancel"
                    Width="45"
                    Click="OnCancelClick">Cancel</Button>
        </StackPanel>
    </StackPanel>
</Window>
